# Copyright (c) Privasys. All rights reserved.
# Licensed under the GNU Affero General Public License v3.0. See LICENSE file for details.

[package]
name = "wasm-precompile"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0"
description = "AOT pre-compiler for WASM components targeting the enclave-os WASM runtime."

[[bin]]
name = "wasm-precompile"
path = "src/main.rs"

[dependencies]
honest-wasmtime-profile = { path = "../../crates/honest-wasmtime-profile", features = ["aot"] }

# Wasmtime with Cranelift — needed to compile, NOT needed at runtime.
# The enclave only deserialises; this tool does the heavy lifting.
wasmtime = { git = "https://github.com/Privasys/wasmtime", tag = "privasys-v0.2.0", default-features = false, features = [
    "cranelift",
    "component-model",
    "runtime",
    "std",
    # Must match the enclave: gc-drc is required to compile the GC /
    # function-references / exception-handling proposals.
    "gc-drc",
] }
